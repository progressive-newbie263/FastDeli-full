import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  
  // cái này giúp tắt đi cái icon nextjs ở góc trái dưới.
  devIndicators: false,
};

export default nextConfig;
